
第一部分(1-396行)
一句话概括
定义整个工具的所有配置、规则和开关，相当于整个工程的大脑
里面主要包括:
路径配置(磁盘、UE、
材质模板
自动化开关
支持导入格式
贴图类型识别关键字
材质参数映射
Compression /sRGB 机则
命名前级
Alias (参数别名)
Manifest、Log 等全局配置
一句话概括 


第二部分(396-515行)
提供统一日志系统，负责打印、统计、保存整个工具运行过程。
里面主要包括:
INFO /WARNING ERROR 三种日志
UE Log 输出
VS Code 控制台输出
成功/警告/错误计数
记录运行耗时
保存ImportLog.txt



第三部分(也是最重要的一部分)515-737
接下来写 utils.py
它以后负责:
扫描所有贴图
识别 BaseColor/Normal/ ORM..
判断 Compression 类型
判断 SRGB
UE 路径转换
文件名规范化
自动加 T
检查非法字符
断支持格式
后续所有模块都会调用这里的函数
这一部分是整个工具的”发动机”，后面的导入、材质绑定、DryRun 全都会依赖它。
**另外一个小建议:**不要把所有代码都放在
轻松得多，也更符合 UE Python 工具的常见组织方式
一个import unreal.py 文件里。从第三部分开始，建议按
我们之前规划拆成Config.py、 Logger.py、 utils.py .....多个文件。这样后续维护、调试和复用都会



